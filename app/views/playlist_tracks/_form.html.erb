<%= form_with(model: playlist_track) do |form| %>
  <% if playlist_track.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(playlist_track.errors.count, "error") %> prohibited this playlist_track from being saved:</h2>

      <ul>
        <% playlist_track.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :playlist_id, style: "display: block" %>
    <%= form.text_field :playlist_id %>
  </div>

  <div>
    <%= form.label :track_id, style: "display: block" %>
    <%= form.text_field :track_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
